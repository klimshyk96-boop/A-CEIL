/* Цей файл раніше визначав обробку витяжки як окремий "чернетковий"
   потік (постановка мітки -> ввід координат X/Y -> застосувати/відміна),
   плюс обгортки setLightMode() і closeModal() для цього потоку.

   Все це стало недосяжним:
   - window.setLightMode: обгортка з 081-inline.js (завантажується
     пізніше) перехоплює ВСІ виклики для типу "витяжка" ще до того, як
     вони дійшли б сюди, і веде на власний редактор (window.openExhaustEditor).
     Для звичайних типів ця обгортка й так завжди просто передавала виклик
     далі — прибрана без втрати поведінки.
   - window.closeModal: умова тут була "якщо є активна чернетка витяжки"
     (draftIndex()>=0), яка ніколи не ставала істинною після втрати
     setLightMode-гілки вище — тобто завжди й так просто передавала
     виклик далі.
   - window.A·CEILFixtureLightCount: перевизначена (і покращена) в
     082-inline.js, яка завантажується пізніше.
   - window.A·CEILIsExhaustLightType, A·CEILCancelExhaustDraft,
     A·CEILApplyExhaustDraft: ніде більше в коді не викликаються —
     останні дві використовувались лише кнопками у HTML, який більше
     ніколи не рендериться.

   Файл лишено на місці (щоб не чіпати index.html і порядок завантаження
   скриптів) як порожній — нічого більше сюди додавати не треба. */
